# README improvement plan — linkedin-jobs-scraper

_Generated 2026-05-16. Concrete actions ordered by priority. Apply via the workspace's README pipeline (`profile.yaml` → `generate-readme.mjs`), not by hand-editing README.md._

## Conversion score: 9/10

Missing components: honestLimitations

## Actions

### 1. 🟡 P2
`startUrls` is exposed but README does not describe paste-mode. Add `paste-mode` to `keyFeatures`. Body source: `docs-standard-copy/multiple-starturls-dedupe.md`.

### 2. 🔵 P3
README has no Limitations / Honest-disclosure section. Add the canonical block from `docs-standard-copy/partial-data-failed-details.md` (Limitations section) via `docs/README_OVERRIDES.json` or a catalog feature.


## Safety notes

- The workspace's HARD RULE is "never hand-edit README.md". The README is auto-generated by `_tools/generate-readme.mjs` from `profile.yaml` + `catalog.yaml` + `README_OVERRIDES.json`.
- To change copy: edit `.actor/profile.yaml` (per-actor) or `_lib/key-features/catalog.yaml` (workspace-shared), then regenerate.
- `generate-readme.mjs --check` reports drift; `--force` overwrites.

## Verification

After regenerating, run:

```bash
node _tools/generate-readme.mjs linkedin-jobs-scraper --check
# Expect: {"drift":false,"changedLines":0,...}
```

Then visually scan the new README for any unsupported claims that snuck through.
