# HTML Inventory

> **Purpose:** This file lists every structured DOM element found in the test fixtures.
> **Rule:** Every entry must be either referenced in `parseJobCards` / `enrichFromDetailHtml`
> OR marked SKIP in `FIELD_COVERAGE.md` with a concrete reason.
> Enforced by `Gate 2b — INVENTORY COVERAGE` in `pre-push-check.mjs`.
>
> Generated: 2026-06-18T14:52:51.252Z

## SERP fixture: `linkedin_serp.html`

> ⚠ No card pattern matched. Update `inventorySerpCards` with a target-specific selector.

## Detail fixture: `linkedin_detail.html`

> No structured metadata detected. If the detail page has rich content, update `inventoryDetailPage`.
